// StatKPI is now merged into AlertStrip.
// This file is kept so any existing imports don't break,
// but its content is intentionally empty — the KPI strip
// is rendered by AlertStrip's top row.
//
// You can safely delete this file once you've confirmed
// VehicleSidebar no longer imports it.

export function StatKPI() {
  return null
}
